---
import Layout from "../../layouts/Layout.astro";
import HeroCarousel from "../../components/HeroCarousel.astro";
import ServicesSlider from "../../components/ServicesSlider.astro";
import AerialGrid from "../../components/AerialGrid.astro";
import WorksGallery from "../../components/WorksGallery.astro";
import LeadGenCTA from "../../components/LeadGenCTA.astro";
import { getCollection } from "astro:content";

const allProjects = await getCollection("projects");
const projects = allProjects.filter((p) => p.data.status === "ejecutado");
---

<Layout title="Proyectos Ejecutados | Savec">
    <div class="pt-20">
        <HeroCarousel projects={projects} />
        <ServicesSlider />
        <AerialGrid projects={projects} />
        <WorksGallery projects={projects} />
        <LeadGenCTA />
    </div>
</Layout>
